
const middlewares = {
    /*
        isLoggedIn : function (req, res, next) {
        if (req.user) return next();
        res.redirect('/');
    }
     */
    render : function(){
        
    }
};

module.exports = middlewares;